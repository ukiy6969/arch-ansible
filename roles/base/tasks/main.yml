---
- name: Install Zsh
  pacman:
    name: ['zsh', 'zsh-completions']
    state: present

- name: Change Shell
  user:
    name: "{{ user.name }}"
    shell: /bin/zsh
    append: yes

- include: locale.yml

- name: Install Tools
  pacman:
    name: "{{ base_packages }}"
    state: present

- include: yay.yml

- name: Install Dev tools
  pacman:
    name:
      - nodejs
    state: present

- include: neovim.yml

- include: dotfiles.yml

- name: Scala tools
  pacman:
    name: sbt
    state: present
